const char = 'abc';

// for (let v of char) {
//   console.log(v);
// }

// console.log(char[Symbol.iterator]);

// @@iterator 必须在它对象或者其原型链上某个对象上有一个键为 @@iterator
// [Symbol.iterator] 一个无参数的的函数 返回值是一个符合(迭代器协议)的 对象

// next() 必须返回一个对象 而且对象应当有了两个属性 value 和 done
